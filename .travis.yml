---
language: python
python: "2.7"
sudo: true

before_install:
  - sudo apt-get update -qq

install:
  - pip install ansible

script:
  ansible-playbook --inventory localhost, --connection local playbook.yml
